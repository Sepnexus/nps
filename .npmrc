# Disable pnpm v11's 24-hour supply-chain delay so freshly-published transitive
# deps don't block CI / Docker builds. We trust the lockfile we committed.
minimum-release-age=0
auto-install-peers=true
